/* include for LSST pipeline harness */

#define MAX_PATH_SIZE 256

/* environment var naming directory with all policies for current pipeline */
#define LSST_POLICY_DIR "LSST_POLICY_DIR"

/* max number of compute nodes allocated in a single pipeline */
#define MAX_NODE_COUNT 200

/* max number of bytes in a node's name */
#define MAX_NODE_NAME_SIZE 256
 
/* max number of bytes in a command string*/
#define MAX_COMMAND_SIZE 512

/* max number of stages in a single pipeline */
#define MAX_STAGES 16

/* max number of bytes in a single environment variable */
#define MAX_ENV_VAR_SIZE 512
