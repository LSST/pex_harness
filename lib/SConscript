# -*- python -*-

Import("env")

env.Replace(SHLIBPREFIX=["lib"]);

env.SharedLibrary('pex_harness', map(lambda str: "../src/" + str,
                          ["Pipeline.cc", 
                           "Slice.cc", 
                           "Queue.cc", 
                           "Stage.cc", 
                           "Clipboard.cc"]))

